node_modules
dist
.env
*.log
.DS_Store

# Local Docker overrides (optional)
docker-compose.override.yml

# TSOA codegen — regenerated by `pnpm generate` / `pnpm dev`
src/generated/routes.ts
src/generated/swagger.json
