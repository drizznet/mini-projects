PORT=4400
CORS_ORIGIN=http://localhost:4200,http://localhost:4300
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
# TRUST_PROXY=true
ADMIN_EMAIL=admin@drizznet.local
ADMIN_PASSWORD=drizznet
AUTH_SECRET=dev-secret-change-me
AUTH_TOKEN_TTL_SEC=86400
GOOGLE_CLIENT_ID=

# --- Pick ONE Postgres target (both can run at once; different ports) ---
# Brew (default): postgresql://YOUR_MAC_USER@localhost:5432/server_dev
DATABASE_URL=postgresql://idrisifeoluwa@localhost:5432/server_dev
# Docker Compose (pnpm docker:up → host port 5433):
# DATABASE_URL=postgresql://server:server@localhost:5433/server_dev
