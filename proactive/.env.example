# Proactive runs fully offline on localStorage by default.
# Fill these in to switch the persistence layer to Supabase
# (see src/lib/supabase/repository.ts and supabase/schema.sql).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
